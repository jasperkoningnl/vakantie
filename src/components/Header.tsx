import{useLocation}from'react-router-dom';import EmergencyButton from './EmergencyButton';import{trip}from'../data/trip';
const names:Record<string,string>={vandaag:'Vandaag','zin-in':'Zin in',plan:'Plan',dagboek:'Dagboek',nood:'Nood',plekken:'Plekken',reisinfo:'Reisinfo',thuisfront:'Thuisfront',checklists:'Checklists',medisch:'Medisch'};
export default function Header(){const key=useLocation().pathname.split('/')[1]||'vandaag';return <header className="sticky top-0 z-[900] border-b border-amber-100 bg-orange-50/95 px-4 py-3 backdrop-blur no-print"><div className="mx-auto flex max-w-4xl items-center justify-between gap-3"><div><p className="text-xs uppercase tracking-wide text-amber-800">{trip.region}</p><h1 className="text-lg font-bold text-stone-900">{trip.title}</h1><p className="text-sm text-stone-600">{names[key]??'Vandaag'} · {trip.destination}</p></div><EmergencyButton/></div></header>}
