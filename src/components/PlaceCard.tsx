import{ExternalLink,MapPin}from'lucide-react';import type{Place}from'../types';
export default function PlaceCard({place,onSelect}:{place:Place;onSelect?:(p:Place)=>void}){return <article className={`card ${place.todo?'todo':''}`}><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold text-stone-900">{place.name}</h3><p className="mt-1 text-sm text-stone-700">{place.description}</p></div>{place.driveTimeMinutes!==undefined&&<span className="tag whitespace-nowrap">{place.driveTimeMinutes} min</span>}</div><div className="mt-3 flex flex-wrap gap-2">{place.tags.map(t=><span className="tag" key={t}>{t}</span>)}{place.lenaScore&&<span className="tag">Lena {place.lenaScore}/5</span>}{place.vegetarianFriendly&&<span className="tag">vegetarisch</span>}</div>{place.notes?.map(n=><p key={n} className="mt-2 text-xs text-stone-500">{n}</p>)}<div className="mt-3 flex flex-wrap gap-2">{onSelect&&place.coordinates&&<button className="rounded-xl bg-stone-100 px-3 py-2 text-sm" onClick={()=>onSelect(place)}><MapPin size={16} className="inline"/> Toon op kaart</button>}{place.links?.googleMaps&&<a className="rounded-xl bg-teal-50 px-3 py-2 text-sm font-semibold text-teal-800" href={place.links.googleMaps} target="_blank" rel="noreferrer"><ExternalLink size={16} className="inline"/> Google Maps</a>}</div></article>}
