import{Car,ExternalLink,MapPin,Star}from'lucide-react';import type{Place}from'../types';
const colors:Record<string,string>={lake:'bg-[#7df474] text-green-900',woods:'bg-green-100 text-green-900',castle:'bg-yellow-300 text-yellow-950',patisserie:'bg-violet-200 text-violet-900',hospital:'bg-red-100 text-red-800',stay:'bg-[color:var(--sage)] text-[color:var(--green)]',restaurant:'bg-orange-100 text-orange-900',supermarket:'bg-emerald-100 text-emerald-900',fuel:'bg-amber-100 text-amber-900'};
export default function PlaceCard({place,onSelect}:{place:Place;onSelect?:(p:Place)=>void}){return <article className={`card group ${place.todo?'todo':''}`}><div className="flex items-start gap-4"><div className={`flex size-12 shrink-0 items-center justify-center rounded-xl font-black ${colors[place.category]??'bg-stone-100 text-stone-700'}`}>{place.category.slice(0,1).toUpperCase()}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><div><h3 className="text-xl font-black text-stone-950">{place.name}</h3><p className="mt-1 text-xs font-extrabold uppercase tracking-[.16em] text-[color:var(--brown)]">{place.category}</p></div>{place.lenaScore&&<span className="inline-flex items-center gap-1 text-[color:var(--olive-dark)]"><Star size={16}/>{place.lenaScore.toFixed(1)}</span>}</div><p className="mt-3 leading-relaxed text-stone-700">{place.description}</p></div></div><div className="mt-4 flex flex-wrap gap-2">{place.tags.map(t=><span className="tag" key={t}>{t}</span>)}{place.vegetarianFriendly&&<span className="tag">vegetarisch</span>}</div>{place.notes?.map(n=><p key={n} className="mt-3 text-xs text-stone-500">{n}</p>)}<div className="mt-4 flex items-center justify-between border-t border-stone-200 pt-4 text-sm">{place.driveTimeMinutes!==undefined&&<span className="inline-flex items-center gap-2"><Car size={16}/>{place.driveTimeMinutes} min</span>}<div className="ml-auto flex gap-2">{onSelect&&place.coordinates&&<button className="rounded-xl bg-stone-100 px-3 py-2 font-semibold" onClick={()=>onSelect(place)}><MapPin size={16} className="inline"/> Toon</button>}{place.links?.googleMaps&&<a className={`rounded-xl px-3 py-2 font-bold ${place.category==='hospital'?'bg-red-50 text-red-700':'bg-[#fff4cf] text-[color:var(--olive-dark)]'}`} href={place.links.googleMaps} target="_blank" rel="noreferrer"><ExternalLink size={16} className="inline"/> Google Maps</a>}</div></div></article>}
