import{BriefcaseMedical,Map,Route,ShoppingBag,Sun}from'lucide-react';import{NavLink}from'react-router-dom';
const items=[['/vandaag','Vandaag',Sun],['/kaart','Kaart',Map],['/route','Route',Route],['/praktisch','Praktisch',ShoppingBag],['/medisch','Medisch',BriefcaseMedical]] as const;
export default function BottomNav(){return <nav className="mobile-only no-print fixed inset-x-0 bottom-0 z-[1000] rounded-t-3xl border-t border-[color:var(--line)] bg-white/95 px-4 py-3 shadow-2xl backdrop-blur"><div className="mx-auto flex max-w-xl justify-between gap-1">{items.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`flex min-w-16 flex-col items-center rounded-full px-3 py-2 text-xs font-semibold transition ${isActive?'bg-[color:var(--sage)] text-[color:var(--green)]':'text-stone-700'}`}><Icon size={22}/><span>{label}</span></NavLink>)}</div></nav>}
