import{Archive,Car,CheckSquare,HeartPulse,Home,Map,MapPinned,Route,ShoppingBasket,Sun}from'lucide-react';import{NavLink}from'react-router-dom';
const items=[['/vandaag','Vandaag',Sun],['/verblijf','Verblijf',Home],['/kaart','Kaart',Map],['/route','Route',Route],['/praktisch','Praktisch',ShoppingBasket],['/thuisfront','Thuisfront',MapPinned],['/medisch','Medisch',HeartPulse],['/checklists','Checklists',CheckSquare],['/archief','Archief',Archive]] as const;
export default function BottomNav(){return <nav className="no-print fixed inset-x-0 bottom-0 z-[1000] border-t border-amber-100 bg-white/95 px-2 py-2 shadow-lg backdrop-blur"><div className="mx-auto flex max-w-4xl gap-1 overflow-x-auto">{items.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`flex min-w-20 flex-col items-center rounded-2xl px-2 py-2 text-xs ${isActive?'bg-teal-700 text-white':'text-stone-600'}`}><Icon size={20}/><span>{label}</span></NavLink>)}</div></nav>}
