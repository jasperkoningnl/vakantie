import{BookOpen,CalendarDays,HeartPulse,Lightbulb,Menu,Sun}from'lucide-react';import{NavLink}from'react-router-dom';
const primary=[['/vandaag','Vandaag',Sun],['/zin-in','Zin in',Lightbulb],['/plan','Plan',CalendarDays],['/dagboek','Dagboek',BookOpen],['/nood','Nood',HeartPulse]] as const;
const secondary=[['/plekken','Plekken'],['/reisinfo','Reisinfo'],['/thuisfront','Thuisfront'],['/checklists','Checklists']] as const;
export default function BottomNav(){return <nav className="no-print fixed inset-x-0 bottom-0 z-[1000] border-t border-amber-100 bg-white/95 px-2 py-2 shadow-lg backdrop-blur"><div className="mx-auto flex max-w-4xl flex-col gap-2"><div className="flex gap-1 overflow-x-auto">{primary.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>`flex min-w-20 flex-1 flex-col items-center rounded-2xl px-2 py-2 text-xs ${isActive?'bg-teal-700 text-white':'text-stone-600'}`}><Icon size={20}/><span>{label}</span></NavLink>)}</div><details className="px-1"><summary className="flex cursor-pointer items-center gap-1 text-xs font-semibold text-stone-600"><Menu size={14}/> Meer</summary><div className="mt-2 flex flex-wrap gap-2">{secondary.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`tag ${isActive?'bg-teal-700 text-white':''}`}>{label}</NavLink>)}</div></details></div></nav>}
