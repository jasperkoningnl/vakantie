import MapView from '../components/MapView';import{places}from'../data/places';import{routeStops}from'../data/route';
export default function RoutePage(){return <><section className="card"><h2 className="text-2xl font-bold">Route</h2><p className="mt-2 text-stone-700">Een rustige tijdlijn voor heenreis, verblijf en terugreis.</p></section><section className="space-y-3">{routeStops.map(s=><article className="card print-card" key={s.id}><p className="tag inline-block">{s.date}</p><h3 className="mt-2 text-xl font-bold">{s.title}</h3><p className="mt-1">{s.from} → {s.to}</p>{s.overnight&&<div className="mt-3 rounded-2xl bg-orange-50 p-3 text-sm"><b>{s.overnight.name}</b>{s.overnight.address.map(a=><span className="block" key={a}>{a}</span>)}</div>}<h4 className="mt-3 font-semibold">Onderweg met Lena</h4>{s.notes.map(n=><p key={n} className="text-sm text-stone-700">• {n}</p>)}<h4 className="mt-3 font-semibold text-orange-800">TODO</h4>{s.todos.map(t=><span key={t} className="tag todo mr-2 mt-2 inline-block">{t}</span>)}</article>)}</section><MapView places={places.filter(p=>['stay','route-stop'].includes(p.category))}/></>}
