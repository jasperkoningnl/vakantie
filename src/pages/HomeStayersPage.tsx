import PrintButton from '../components/PrintButton';import{emergencyInstructions,homeStayerNote}from'../data/contacts';import{routeStops}from'../data/route';import{mainAccommodation,trip}from'../data/trip';
export default function HomeStayersPage(){return <><section className="card print-card"><div className="flex items-start justify-between gap-3"><div><h2 className="text-2xl font-bold">Thuisfront-overzicht</h2><p className="mt-2 text-stone-700">Essentiële informatie voor wie thuis blijft.</p></div><PrintButton/></div></section><section className="grid gap-3 sm:grid-cols-2"><div className="card print-card"><h3 className="font-bold">Reis</h3><p>{trip.travelDates}</p><p>{trip.travellers.join(', ')} · Lena is {trip.lenaAge}</p><p className="mt-2 font-semibold">{homeStayerNote}</p></div><div className="card print-card"><h3 className="font-bold">Auto</h3><p>{trip.car.color} {trip.car.model}</p><p className="text-2xl font-black tracking-wide">{trip.car.licensePlate}</p><p>{trip.car.source}</p><img src={trip.car.photo} onError={e=>{e.currentTarget.style.display='none'}} className="mt-3 rounded-2xl" alt="Honda CR-V"/></div></section><section className="card print-card"><h3 className="font-bold">Hoofdverblijf</h3><p>{mainAccommodation.name}</p>{mainAccommodation.address.map(a=><p key={a}>{a}</p>)}<p className="mt-2">{mainAccommodation.phone} · {mainAccommodation.mobile}</p><p>{mainAccommodation.email}</p></section><section className="space-y-3">{routeStops.map(s=><article className="card print-card" key={s.id}><b>{s.date}: {s.from} → {s.to}</b>{s.overnight&&<div className="mt-2 text-sm"><p>{s.overnight.name}</p>{s.overnight.address.map(a=><p key={a}>{a}</p>)}</div>}</article>)}</section><section className="danger-card print-card"><h3 className="font-bold">Noodinstructies</h3>{emergencyInstructions.map(i=><p className="mt-2" key={i}>• {i}</p>)}</section></>}
