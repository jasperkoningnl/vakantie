import{ExternalLink}from'lucide-react';import{mainAccommodation,stayHighlights,stayPeriods}from'../data/trip';
export default function StayPage(){return <><section className="card"><h2 className="text-2xl font-bold">{mainAccommodation.name}</h2><p className="mt-2 text-stone-700">Ons rustige thuis voor twee weken in de Lot / Quercy Blanc.</p><address className="mt-3 not-italic text-sm">{mainAccommodation.address.map(a=><span key={a} className="block">{a}</span>)}</address><div className="mt-3 grid gap-2 text-sm"><a href={`tel:${mainAccommodation.phone}`}>{mainAccommodation.phone}</a><a href={`tel:${mainAccommodation.mobile}`}>{mainAccommodation.mobile}</a><a href={`mailto:${mainAccommodation.email}`}>{mainAccommodation.email}</a><a className="font-semibold text-teal-800" href={mainAccommodation.website} target="_blank" rel="noreferrer"><ExternalLink size={16} className="inline"/> Website</a></div></section><section className="grid gap-3 sm:grid-cols-2">{stayPeriods.map(p=><article className="card" key={p.name}><h3 className="font-bold">{p.dates}</h3><p className="text-lg">{p.name}</p>{p.notes.map(n=><p className="mt-1 text-sm text-stone-600" key={n}>• {n}</p>)}</article>)}</section><section className="card"><h3 className="font-bold">Fijn om te weten</h3><div className="mt-2 flex flex-wrap gap-2">{stayHighlights.map(h=><span className="tag" key={h}>{h}</span>)}</div><div className="mt-4 rounded-2xl bg-orange-50 p-4 text-sm text-stone-600">Foto’s kunnen later in <code>/public/images</code> worden toegevoegd. De pagina blijft bruikbaar als beelden ontbreken.</div></section></>}
